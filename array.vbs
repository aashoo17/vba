Option Explicit

Sub Arrays()
    Dim h(20) As Integer
    ' loop over array
    For Each i in h
        ' do something here
    Next
    ' TODO: accessing element index during looping over arrays
End Sub